{
  "name": "nvidia-proxy",
  "version": "1.0.0",
  "scripts": {
    "start": "npx @ai-ql/api-proxy --target=https://integrate.api.nvidia.com --port $PORT"
  },
  "dependencies": {
    "@ai-ql/api-proxy": "^1.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
